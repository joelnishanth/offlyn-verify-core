version: "3.8"

services:
  verify-core:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/app
    working_dir: /app
    command: python -m planner.scenarios --scenario all
